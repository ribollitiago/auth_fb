import 'package:flutter/material.dart';

class ScheduleTemporaryPage extends StatelessWidget {
  const ScheduleTemporaryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('CONSULTAS'),
      ),
    );
  }
}
